package engine

// Engine is the core execution entry point.
// It will eventually hold the state machine, policy engine, and registry.
type Engine struct {
	// Stub: Add fields as needed later
}

// NewEngine creates a new instance of the Engine.
func NewEngine() *Engine {
	return &Engine{}
}
